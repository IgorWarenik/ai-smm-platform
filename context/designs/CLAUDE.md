# Design System

Visual design constraints for user-facing work.

## Conventions
- If a root `DESIGN.md` exists, treat it as canonical.
- If no design system exists yet, create one before implementing the frontend.
- Frontend work should reference design tokens and interaction patterns from this tier.
