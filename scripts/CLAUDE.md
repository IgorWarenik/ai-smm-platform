# Scripts

Utility scripts for setup, deployment, linting, tests, and validation.

## Conventions
- Scripts should be idempotent where practical.
- Do not store secrets or generated credentials in scripts.
- Keep token-economy validation aligned with `context/kits/cavekit-tokens.md`.
