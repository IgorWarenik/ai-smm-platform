# Apps

Application surfaces for the platform.

## Conventions
- `apps/api/` contains the Fastify backend.
- `apps/workflows/` contains n8n workflow code.
- Future frontend work should live under `apps/frontend/` and trace behavior to `context/kits/`.
