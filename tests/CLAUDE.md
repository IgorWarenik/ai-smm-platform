# Tests

Repository-level tests and integration coverage.

## Conventions
- Use `WORKPLAN.md` Stage 2 as the current checklist.
- Prefer acceptance-criteria-shaped tests from `context/kits/`.
- Keep project isolation tests explicit for multi-tenant behavior.
